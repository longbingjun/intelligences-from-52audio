name: 爬虫更新数据 + 构建并部署静态站点

on:
  schedule:
    - cron: "10 1 * * *"
  workflow_dispatch:
    inputs:
      crawl_mode:
        description: daily=日更首页；backfill-2026=补全2026年
        type: choice
        options:
          - daily
          - backfill-2026
        default: daily
      run_merge_ocr:
        description: 是否合并 OCR 结果到 BOM（较慢，默认 false）
        type: boolean
        default: false
      run_identity_repair:
        description: 从我爱音频网原文增量提取待治理产品的品牌与完整型号（消耗 DeepSeek 配额）
        type: boolean
        default: false
  push:
    branches: [main, master]

permissions:
  contents: write
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  crawl-and-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
          cache-dependency-path: requirements.txt

      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: web/package.json

      - name: 安装依赖
        run: pip install -r requirements.txt

      - name: 运行 v2 爬虫
        if: github.event_name != 'push'
        run: |
          MODE="${{ github.event.inputs.crawl_mode || 'daily' }}"
          python scripts/crawl_v2.py --mode "$MODE"

      - name: 增量治理产品身份
        if: github.event_name == 'workflow_dispatch' && github.event.inputs.run_identity_repair == 'true'
        env:
          DEEPSEEK_API_KEY: ${{ secrets.DEEPSEEK_API_KEY }}
        run: python scripts/repair_product_identities.py --all --workers 2 --write-overrides

      - name: 可选预处理（搜索索引 / 产品库 / 矩阵 / 信源标签）
        run: |
          for script in build_search_index build_products build_identity_review build_product_priority summarize_selling_points summarize_roundup_insights build_matrix tag_source_layer; do
            if [ -f "scripts/${script}.py" ]; then
              echo "Running scripts/${script}.py"
              if [ "$script" = "summarize_selling_points" ] || [ "$script" = "summarize_roundup_insights" ]; then
                DEEPSEEK_API_KEY="${{ secrets.DEEPSEEK_API_KEY }}" python "scripts/${script}.py"
              else
                python "scripts/${script}.py"
              fi
            fi
          done

      - name: 可选 OCR 合并（手动触发 workflow 时）
        if: github.event_name == 'workflow_dispatch' && github.event.inputs.run_merge_ocr == 'true'
        run: python scripts/merge_ocr.py --all

      - name: 同步前端数据并构建站点
        run: |
          python scripts/prepare_web_data.py
          cd web
          npm install
          npm run build
          cd ..
          python scripts/generate_legacy_redirects.py
          touch site/.nojekyll

      - name: 上传 Pages 产物
        uses: actions/upload-pages-artifact@v3
        with:
          path: site

      - name: 提交数据变更
        # 仅回写原始采集数据与 AI 缓存。products/matrix/web/public 等都是
        # 每次构建可再生的派生产物；把它们同时提交会让两次运行在 rebase 时
        # 对同一份 JSON 发生无意义冲突，进而阻断 Pages 部署。
        if: github.event_name != 'push' || !contains(github.event.head_commit.message, '[skip ci]')
        run: |
          git config user.name "intel-bot"
          git config user.email "intel-bot@users.noreply.github.com"
          git add data/index.json data/reports/ data/videos/ data/enrich/
          git diff --cached --quiet || git commit --trailer "Co-authored-by: Cursor <cursoragent@cursor.com>" -m "chore: 自动更新情报数据 v2 [skip ci]"
          git pull --rebase --autostash origin master
          git push

  deploy:
    needs: crawl-and-build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: 部署 GitHub Pages（失败自动重试）
        id: deployment
        uses: Wandalen/wretry.action@v3.5.0
        with:
          action: actions/deploy-pages@v4
          attempt_limit: 5
          attempt_delay: 45000
