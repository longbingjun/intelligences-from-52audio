# Legacy V4 static-site generator

This directory preserves the pre-Astro renderer for history only. The active
site is `web/` and is built through `python scripts/pipeline.py site`.

The archived scripts are intentionally excluded from CI and are not maintained
as a second production path. Use Git history if a V4 page needs to be restored.
